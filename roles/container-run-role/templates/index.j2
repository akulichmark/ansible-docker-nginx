<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>General</title>
    <style>
        /* Basic reset for margin and padding */
        body,
        html {
            margin: 0;
            padding: 0;
            height: 100%;
            font-family: Arial, sans-serif;
            background-color: #f6f6f6;
            /* Light gray background */
        }

        /* Flexbox styles to center the content */
        .container {
            display: flex;
            justify-content: center;
            /* Center horizontally */
            align-items: center;
            /* Center vertically */
            height: 100%;
            /* Full height of the viewport */
        }

        /* Card styles for the centered content */
        .card {
            background-color: white;
            /* White background for the card */
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            /* Subtle shadow for depth */
            text-align: center;
            /* Center text inside the card */
            max-width: 400px;
            /* Maximum width for the card */
            width: 100%;
            /* Full width on smaller screens */
        }

        /* Button styles */
        .btn {
            background-color: #007bff;
            /* Bootstrap primary color */
            color: white;
            /* White text */
            padding: 10px 15px;
            border: none;
            border-radius: 5px;
            text-decoration: none;
            /* No underline */
            font-size: 16px;
            cursor: pointer;
            margin: 10px 0;
            /* Margin for spacing between buttons */
            display: inline-block;
            /* Ensure the button is inline-block */
            transition: background-color 0.3s;
            /* Smooth transition for hover effect */
        }

        .btn:hover {
            background-color: #0056b3;
            /* Darker blue on hover */
        }

        /* Responsive adjustments */
        @media (max-width: 500px) {
            .card {
                padding: 15px;
                /* Reduce padding on smaller screens */
            }
        }
    </style>
</head>


<body>
    <div class="container">
        <div class="card">
            <h1>Welcome to general</h1>
            <p>Click at the button to access the required link!</p>
            <a href="http://{{ansible_host}}/music/" class="btn">music</a>
            <a href="http://{{ansible_host}}/image/" class="btn">image</a>
            <a href="http://{{ansible_host}}/proxy/" class="btn">proxy</a>
            <a href="http://{{ansible_host}}/adonded/" class="btn">adonded</a>
        </div>
    </div>
</body>

</html>
